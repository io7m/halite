language: java

jdk:
  - oraclejdk11
  - oraclejdk10

install: true

script:
  - mvn --errors clean verify site
  - bash <(curl -s https://codecov.io/bash) -f ./com.io7m.halite.tests/target/site/jacoco-aggregate/jacoco.xml

notifications:
  irc: "chat.freenode.net#io7m"

